# F05 同 SHA 三项验收回执（48d8376）

- 核验日期：2026-09-23
- 完整 SHA：`48d837692b81e56bd88e13189f0cf15ee34976d3`
- 当前判定：**三项同 SHA 验收均通过，F05-A07、F05-A09 关闭**。验收范围及目标环境性能边界见下文。

## 三项回执

| 验收面 | 回执 | 结果与范围 |
|---|---|---|
| QuantOS CI #117 | [Run 35881789334](https://github.com/SumAlphaAI/QuantOS/actions/runs/35881789334)、[CI 结构化核验摘录](./evidence/F05-ci-48d8376-pass.json) | **SUCCESS**；`verify`、`signing-policy`、`sign-main`、`verify-download-main`、`verify-download` 均成功；主干不适用的 `verify-download-pr` 按预期跳过。正式下载验签日志确认 236 个发布文件与 manifest HMAC-SHA256 签名。 |
| F05 Event Nightly #7 | [Run 35882055545](https://github.com/SumAlphaAI/QuantOS/actions/runs/35882055545)、[原始制品摘录](./evidence/F05-nightly-48d8376-pass.json) | **SUCCESS / PASS**；全新 PostgreSQL 重建、RLS、数据库集成、10,000 条真实消费者、HTTP 故障与完整覆盖率门禁均通过 |
| 隔离 Supabase `f05-target-coverage` | [原始结构化回执](./evidence/F05-target-48d8376-pass.json) | **PASS**；`dirty=false`，重置隔离 schema 并重建 15 项 migration；PostgreSQL、RLS、真实 Storage 往返、10,000 条正式消费者一致性均通过 |

Nightly 制品 `10761505421` 的 GitHub 页面摘要为 `sha256:f7b0a01178f0350ce911db3c0de7420f360934d804575c669f94f1a489387bc4`。制品内 `database.json` 为 `PASS`、`source` 为上述完整 SHA、`dirty=false`；六文件清单的 line **2846/2951（96.44%）**、region **2217/2430（91.23%）**、branch **120/138（86.96%）**。`quantos-event/src/pg.rs` Linux 单文件 region **607/714（85.014%）**，按原始计数越过 85% 门槛，没有降低阈值或排除源文件。

Nightly `measurements.json` 记录 `eventCount=10000`、`uniqueSideEffects=10000`、`dispatched=10000`、`appliedReceipts=10000`、`checkpointNextSequence=10001`。正式 `PgEventStore::poll_outbox_once` 消费 **3314ms**；`lookupScope=complete-client-retrieval` 的完整载荷客户端取回 **160.233317ms**，满足 ≤5 秒。隔离目标另以 `lookupScope=target-event-id-client-retrieval` 检查真实 pooler 路径上的 ID 全链完整性；目标侧该传输用时 **19749.879625ms**，不得替代 Nightly 的完整载荷性能门禁。

## 验收判定与边界

三项回执均绑定 `48d837692b81e56bd88e13189f0cf15ee34976d3` 且通过：A07 的 Linux 单文件与六文件覆盖率门禁、A09 的 10,000 条正式消费与完整链性能、隔离 Supabase 数据一致性及 CI 正式打包验签均满足本轮验收条件。C16、C19、C27 更新为 PASS，F05 计 30/30 PASS，原 11 项问题全部关闭。

原计划的 ≤5 秒**完整事件链**指标采用 Nightly `complete-client-retrieval` 实测 160.233317ms。隔离 Supabase 的 `target-event-id-client-retrieval` 用于确认目标数据库 ID 链完整，19749.879625ms 是跨区域客户端路径的观测值，既不声称目标环境完整载荷取回 ≤5 秒，也不以其替代 Nightly 指标。若后续将“目标 Supabase 完整载荷端到端 ≤5 秒”纳入正式范围，须在同区域部署测量客户端并独立验收。
