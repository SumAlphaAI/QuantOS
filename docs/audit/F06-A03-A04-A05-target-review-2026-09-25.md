# F06-A03/A04/A05 隔离目标续修与复验

> 日期：2026-09-25。依据 [F06 全面复审](./F06-comprehensive-review-2026-09-24.md)和 [F06 续修记录](./F06-continuation-2026-09-24.md)。目标执行绑定完整源码提交 `1839b98c950c7f2d532b59dea3bac8d2d87fed53`；结构化结果见 [目标回执](./F06-A03-A04-A05-target-receipt-2026-09-25.json)。未记录任何密钥或连接密码。

## 修复与关闭判定

| 问题 | 修复及目标负向证明 | 判定 |
|---|---|---|
| A03 跨租户关联 | 保留现有组合租户外键与 Auth 查询租户谓词；新增跨租户 membership 的 actor/workspace、capability 的 actor/workspace/account、service session 的 actor/account 七种错配写入探针，均收到外键拒绝；`authenticated` RLS 对第二租户读出 0 行，错误租户主上下文拒绝。 | **CLOSED**，7/7 写入拒绝及读取隔离通过。 |
| A04 主上下文与 mode | 保留服务端由唯一 Primary 账户导出 mode 的路径；账户 mode 变化后，BFF 会话重新读取当前账户 mode，原 paper grant 不再进入上下文。请求自带错误 mode 的授权被拒；真实 Supabase Auth→live BFF 的会话、Primary context、隐藏账户、撤销等 9 项 HTTP 检查通过。 | **CLOSED**，目标数据库与正式 live BFF 路径均通过。 |
| A05 capability 范围 | Rust policy 对 `execution.operate` 强制要求账户，调用方省略标志也不能放行。新增 migration 要求 SQL helper 在同一 capability 上必须给出账户，并继续核对账户 mode、活动状态与访问关系。全局 grant 下省略账户、空 mode、错误 mode、其他租户均拒绝；匹配账户与 mode 可通过。Owner 对未知和 secret capability 仍默认拒绝。 | **CLOSED**，本地及隔离目标负向与正向对照通过。 |

## 可重放命令与边界

| 命令 | 本次结果 |
|---|---|
| `cargo fmt --check`、`cargo test -p quantos-policy -p quantos-auth --lib --locked` | PASS；policy 6/6、auth 4/4。 |
| `make db-migration-check` | PASS；迁移命名及 38 表 RLS 静态检查。 |
| `make db-apply` | PASS；隔离库仅应用新增的 `20260925110000` 迁移，其余已应用。 |
| `make f06-live-check` | PostgreSQL harness 9/9 PASS；其中 P95 用例明确 **NOT RUN**，实际数据库测试 8/8。 |
| `make f06-bff-live-smoke` | PASS；真实 Auth 测试身份、专用 BFF 登录、合成 HTTPS Origin 下 9 项 HTTP 检查。 |
| `make db-schema-diff` | **NOT RUN**；缺新建参考库的 `QUANTOS_REFERENCE_DATABASE_URL`，不计为 drift PASS。 |

本次测试使用已配置的隔离 Supabase 项目和测试身份；`DATABASE_URL` 用于 fixture/迁移，live BFF 使用独立登录和严格 TLS。`db-schema-diff` 的缺项不改变已在目标库执行的外键、RLS、mode 与 capability 负向结果，但后续发布前仍应在新建参考库重放迁移漂移检查。

F06 整体仍为 `FIX_VALIDATION`：A10 与同一最终完整 SHA 的总回执尚未关闭。本回执只关闭 A03/A04/A05，不把既有 A01/A02/A06/A07/A08/A09 的历史源码 SHA 自动提升为本 SHA 的总体验收。
