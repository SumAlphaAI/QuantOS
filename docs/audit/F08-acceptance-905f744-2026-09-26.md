# F08 跨秒修复后同 SHA 正式验收

## 验收结论

完整源码 SHA `905f744da8fb4b1987712559e280a44f1a1c3cbf` 的 CI、Nightly 与隔离目标服务均首次执行成功。两道远程 Gate 产物中的 `source-sha.txt`、目标原始回执及工作流 `headSha` 一致；标签 `f08-target-905f744da8fb` 固定该源码基线。F08 开发阶段结论恢复为 **ACCEPTED**，原问题 11/11 关闭，检查点 24/24 完成，三条量化要求有本地与目标场景证明。

[汇总回执](./evidence/f08-905f744/receipt.json)保存三道运行 ID、覆盖率和 19 个原始证据文件摘要；[目标回执](./evidence/f08-905f744/target-receipt.json)保存逐场景结果。历史失败及旧 SHA 回执继续保留。本结论只绑定上述源码 SHA，归档文档提交和后续合并提交不自动继承同 SHA 验收。

## 远程 Gate 明细

| Gate | 运行 | 结果与核验 |
| --- | --- | --- |
| CI | [36236861889](https://github.com/SumAlphaAI/QuantOS/actions/runs/36236861889) | SUCCESS；完整稳定版 Gate；Rust 55 项、Python 134 项；Python 8/8 生产文件覆盖率达标，最低 87.23%；跨秒回归通过；覆盖率负向探针 3 PASS、Nightly 专属项 1 SKIP。 |
| Nightly | [36236861831](https://github.com/SumAlphaAI/QuantOS/actions/runs/36236861831) | SUCCESS；完整分支 Gate；跨秒回归通过；覆盖率负向探针 4 PASS。 |
| 隔离目标 | [36237146224](https://github.com/SumAlphaAI/QuantOS/actions/runs/36237146224) | SUCCESS；在上述两道结果和产物核验完成后推送专用标签触发；九项 release 场景 9/9 PASS。 |

CI 的 Rust 原始 LLVM line/region 为 **95.88%/90.38%**，物理源码行 **97.64%**，豁免后 region **92.88%**。Nightly 原始 LLVM line/region/branch 为 **94.21%/86.84%/87.33%**，物理源码行 **95.92%**，豁免后 region/branch 为 **89.32%/90.07%**。现有阈值、127 项逐源码位置豁免及负向探针均保持；CI 使用 78 项 region 豁免，Nightly 使用 50 项。

隔离目标是新的 Ubuntu 24.04 GitHub runner，Mock Engine 从 wheel 安装到 `site-packages`，私有临时目录权限为 `0700`。wheel SHA-256 为 `6675eab60ecb6d26bdefc20f52406b5fe713a1370d60d4b0d76b160afe2237fd`，下载文件实算与回执一致。九项日志逐一确认实际执行 1 个测试且通过：五 RPC/租户拒绝、三次受监督崩溃、两秒 deadline、运行中取消、持久重建、Manager OS 强杀与重放策略、幂等/改输入、制品篡改拒绝、不可信 RPC 拒绝。

## 剩余边界

跨秒 readiness 误拒绝已在本地证明旧实现失败、修复后通过，并在本轮远程 CI/Nightly 再次通过。原始 `rd-agent` 失败日志未记录请求时间，历史事件不作唯一根因断言；不影响当前修复及三道成功回执的事实。CPU/GPU 硬隔离仍按用户决定移出 F08 Gate；本次目标服务验收不代表生产部署。

## 与 main 合并建议

读取远端比较时，`main` 为 `033eebde3e0e7ad22ea779ba6b59d160e65f9998`，候选分支领先 5 个提交、落后 0 个提交，没有现有 PR。差异为 F08 实现/测试、覆盖率、工作流及审计材料，建议通过 PR 合入 `main`，使后续工作使用已修复的实现。

当前 main 生效规则仅为禁止删除及禁止非快进强推，没有强制状态检查或评审规则；仍应在 PR 上检查仓库通用工作流结果，再执行合并。建议保留提交历史与验收标签。合并若生成新的 SHA，不能把本报告回执改写为该 SHA 的验收；主线检查按合并后的提交运行。本轮只完成验收及合并条件检查，未创建 PR 或执行合并。
