# F05 同 SHA 验收复核（88cf19f）

- 核验日期：2026-09-23
- 完整 SHA：`88cf19fb83f518d6f7bc739068434c738fac5c8d`
- 结论：**暂不关闭 F05**。QuantOS CI 与隔离 Supabase 目标 Gate 成功；F05 Event Nightly 的万条消费与完整链性能达标，但 Linux 单文件 region 覆盖率低于 85%。所有结果均保留同 SHA 边界。

## 三项回执

| 验收面 | 证据 | 结果 |
|---|---|---|
| QuantOS CI #116 | [Run 35872837095](https://github.com/SumAlphaAI/QuantOS/actions/runs/35872837095) | **SUCCESS**；`verify`、`signing-policy`、`sign-main`、`verify-download-main`、`verify-download` 成功；主分支 `verify-download-pr` 按预期跳过；27 个 Playwright 用例通过，正式制品签名与下载验签完成 |
| F05 Event Nightly #6 | [Run 35873319113](https://github.com/SumAlphaAI/QuantOS/actions/runs/35873319113)、[结构化摘录](./evidence/F05-nightly-88cf19f-failure.json) | **FAIL**；`quantos-event/src/pg.rs` 606/714 regions = **84.87%**，至少需要 607/714；门禁正确失败，不能降低阈值 |
| 隔离 Supabase `f05-target-coverage` | [目标原始回执](./evidence/F05-target-88cf19f-pass.json) | **PASS**；干净 SHA、重置隔离 schema、15 项 migration、PostgreSQL/RLS、HTTP 故障、真实 Storage 往返及 10,000 条正式消费者回放通过；目标侧 `pg.rs` 835/966 regions = 86.44% |

Nightly 制品 `10756990544` 的 GitHub 页面摘要为 `sha256:2d7502507ff51bb326bd1467cb24c2fb541e6115e82d1c45b582ff7e1a9a1673`。结构化测量：10,000 条事件、10,000 次唯一副作用、10,000 条 dispatched、10,000 条 applied receipt、checkpoint 10,001；正式 `PgEventStore::poll_outbox_once` 消费 **3.160s**，完整载荷客户端取回 **155.790293ms**，满足 ≤5 秒。Nightly 整体仍因覆盖率失败，不能将性能单项 PASS 移记为 A07、A09 的最终关闭。

目标回执的事件 ID 全链取回为 **22.229s**，`lookupScope=target-event-id-client-retrieval`；该值记录跨区域 session pooler 的客户端传输与数据库实际往返，不代替 Nightly 的完整载荷性能测量，也不用于 Nightly 的 5 秒 Gate。目标侧正式消费者耗时 **723.551s**，计数与 checkpoint 均完整。两类测量的作用域在代码和回执中分别标识。

## 剩余整改

1. 对 Linux `pg.rs` 增加至少 1 个实际覆盖区域，保留 85% 单文件门禁和完整 6 文件清单。新增的无数据库连接失败测试在本机定向执行通过，并对 PostgreSQL 明文及 TLS 连接的错误路径进行验证；本机增量覆盖率从 835/966 提升至 841/966 regions。它尚须由新 SHA 的 Linux Nightly 证明覆盖率。
2. 将修复提交通过 GitHub Desktop 人工推送，收集新完整 SHA 的 QuantOS CI、F05 Event Nightly 以及隔离 Supabase 目标回执；只有三项均为 SUCCESS/PASS 才更新 C16、C19、C27 并关闭 A07、A09。
