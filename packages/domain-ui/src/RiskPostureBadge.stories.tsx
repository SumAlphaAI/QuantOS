import type { Meta, StoryObj } from "@storybook/react"; import { InlineAlert } from "@sumalpha/ui"; import { RiskPostureBadge } from "./indicators";
const meta = { title: "domain-ui/RiskPostureBadge", component: RiskPostureBadge, tags: ["autodocs"], parameters: { docs: { description: { component: "ACC-GS-S4：未知风险枚举 fail closed。loading/empty/error/unauthorized/stale/offline 由父级容器承担。" } } }, args: { posture: "allow", reason: "确定性规则校验通过" } } satisfies Meta<typeof RiskPostureBadge>; export default meta; type Story = StoryObj<typeof meta>;
export const Default: Story = {}; export const ApprovalRequired: Story = { args: { posture: "approval_required", reason: "组合敞口超过无审批阈值" } }; export const Deny: Story = { args: { posture: "deny", reason: "交易所不在授权白名单" } }; export const UnknownFailClosed: Story = { args: { posture: "future_risk_state", reason: "未知枚举阻断高风险动作" } }; export const DangerConfirmBoundary: Story = { render: () => <InlineAlert tone="danger" title="危险确认职责">本徽标只呈现决策；实际危险动作必须进入 DangerConfirmDialog。</InlineAlert> };
