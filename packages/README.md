# packages

Shared TypeScript packages used by the apps live here.

- `ui`: presentational components
- `domain-ui`: domain-aware UI composition
- `api-client`: typed API contracts and fetch helpers
- `platform`: platform adapters for web and desktop
- `config`: shared build and runtime configuration helpers
