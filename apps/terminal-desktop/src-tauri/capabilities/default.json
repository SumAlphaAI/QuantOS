{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal default capability set (最小 capability 原则，执行计划第 2 节)",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
