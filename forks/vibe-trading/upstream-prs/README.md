# TP01 Upstream PR Records

This directory records upstream contribution opportunities discovered during TP01.

Each record should capture:

- the generic bugfix or improvement that may be contributed upstream;
- whether the change is adapter-specific or generally reusable;
- current status (`draft`, `opened`, `merged`, `rejected`, `not-applicable`);
- links or placeholders for the upstream PR, issue, or patch discussion.
