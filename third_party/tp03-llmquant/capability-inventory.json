{
  "task": "TP03",
  "capabilities": [
    {
      "name": "quant.signal.v1",
      "input": "released feature/DataSnapshot reference",
      "output": "QuantOS Signal and diagnostics",
      "sideEffects": ["Artifact API write"],
      "permissions": ["snapshot.read", "artifact.write"],
      "forbidden": ["oms", "venue", "secret", "arbitrary_network"],
      "replacement": "any quant.signal.v1 compatible Engine"
    }
  ]
}
