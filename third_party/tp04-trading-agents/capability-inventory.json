{
  "task": "TP04",
  "capabilities": [
    {
      "name": "decision.proposal.v1",
      "input": "versioned Signal and policy context",
      "output": "non-executable QuantOS TradeProposal with debate evidence",
      "sideEffects": ["Artifact API write", "audit event write"],
      "permissions": ["signal.read", "artifact.write", "audit.write"],
      "forbidden": ["order", "venue", "secret", "arbitrary_network"],
      "replacement": "fixture-backed QuantOS proposal provider"
    }
  ]
}
