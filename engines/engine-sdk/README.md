# engine-sdk

Shared Python helpers and types for QuantOS engines.

Current SDK scope:

- engine manifest dataclasses
- UDS gRPC server registration helpers
- lightweight generic gRPC client for contract tests
- JSON document and timestamp utilities
