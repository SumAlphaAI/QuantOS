# quantos-proto

Generated Rust SDK for QuantOS Protocol v1.

This crate consumes `buf generate` output from `proto/` and exposes typed domain
messages plus client-side gRPC stubs for Engine and Event APIs.
