# quantos-core

Shared Rust foundation crate for QuantOS.

Responsibilities in the F01 baseline:

- expose workspace metadata for smoke tests;
- provide a tiny health report type that downstream services can consume;
- act as the first compilable crate in the workspace.
